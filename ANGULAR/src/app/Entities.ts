export interface Candidat { 
  name: string,
  age : number
}
export interface book {
  id : number,
  title : String,
  author : string,
  price : number,
  dateRelease : string,
  coverImage : string
}